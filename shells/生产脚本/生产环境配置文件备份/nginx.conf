
user  nginx;
worker_processes  1;

#error_log  logs/error.log;
#error_log  logs/error.log  notice;
#error_log  logs/error.log  info;

#pid        logs/nginx.pid;


events {
    worker_connections  1024;
}


http {
    include       mime.types;
    default_type  application/octet-stream;

    #log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
    #                  '$status $body_bytes_sent "$http_referer" '
    #                  '"$http_user_agent" "$http_x_forwarded_for"';

    #access_log  logs/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    #keepalive_timeout  0;
    keepalive_timeout  65;

    #gzip  on;

#负载均衡,暂时不用调用虚地址
#    upstream wxfinacemapping{
#    	#调用虚地址
#			server 192.168.68.194:8130;
#			#分开调用示例
#			#server 192.168.68.178:8130;
#			#server 192.168.68.179:8130;
#			#server 192.168.68.218:8130;
#    	}

    server {
        listen       8080;
        server_name  localhost;
        #上传文件大小限制100M,暂时不用,后期会用
        client_max_body_size 100M;

        #charset koi8-r;

        #access_log  logs/host.access.log  main;
                #wsapi调用地址
                #location ^~ /wsapi/ { 
                #	proxy_pass http://wxfinacemapping/;
                #	proxy_redirect off;
        				#}
        
        				#
	        			location ^~ /wefinance/image/ {
               	 root            /;
               	 #rewrite ^/wefinance/image/(.*)$ /u01/app/nfsfiles/$1 break;
               	 rewrite ^/wefinance/image/(.*)$ /home/nginx/web/file/$1 break;
                }
                
                #router location,H5
                location /wefinance/pages/ {
                    rewrite ^\/wefinance/pages\/(.*?)\/(.*)$ /wxh5app/$1.html last;

                }
                #static location,H5
                location /wefinance/static/ {
                   rewrite ^\/wefinance/static\/(.*?)$ /wxh5app/static/$1 last;

                }
                
								#微信验证文件(武航临时使用)
								location ^~ /wefinance/static/c6zFNSMtFk {
                 root            /;
                 rewrite ^/wefinance/static/c6zFNSMtFk/(.*)$ /home/nginx/web/file/wxfront/static/c6zFNSMtFk/$1 break;
                }
                

								#外网调用地址
                location ^~ /wefinance/{
                	#调用虚地址
               	 proxy_pass http://192.168.68.194:8130/;
               	 #负载均衡
								#	proxy_pass http://wxfinancemapping/;
                proxy_redirect off;
                }

                location   / {
                try_files $uri $uri/ /index.html;

                   root         /home/nginx/web/file;
                   index        index.html index.html;

                }

        #location / {
        #    root   html;
        #    index  index.html index.htm;
        #}

        #error_page  404              /404.html;

        # redirect server error pages to the static page /50x.html
        #
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   html;
        }

        # proxy the PHP scripts to Apache listening on 127.0.0.1:80
        #
        #location ~ \.php$ {
        #    proxy_pass   http://127.0.0.1;
        #}

        # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
        #
        #location ~ \.php$ {
        #    root           html;
        #    fastcgi_pass   127.0.0.1:9000;
        #    fastcgi_index  index.php;
        #    fastcgi_param  SCRIPT_FILENAME  /scripts$fastcgi_script_name;
        #    include        fastcgi_params;
        #}

        # deny access to .htaccess files, if Apache's document root
        # concurs with nginx's one
        #
        #location ~ /\.ht {
        #    deny  all;
        #}
    }


    # another virtual host using mix of IP-, name-, and port-based configuration
    #
    #server {
    #    listen       8000;
    #    listen       somename:8080;
    #    server_name  somename  alias  another.alias;

    #    location / {
    #        root   html;
    #        index  index.html index.htm;
    #    }
    #}


    # HTTPS server
    #
    #server {
    #    listen       443 ssl;
    #    server_name  localhost;

    #    ssl_certificate      cert.pem;
    #    ssl_certificate_key  cert.key;

    #    ssl_session_cache    shared:SSL:1m;
    #    ssl_session_timeout  5m;

    #    ssl_ciphers  HIGH:!aNULL:!MD5;
    #    ssl_prefer_server_ciphers  on;

    #    location / {
    #        root   html;
    #        index  index.html index.htm;
    #    }
    #}

}
